# 智攻 (SmartAttack) 后端依赖
# 安装: pip install -r requirements.txt

Flask>=3.0
flask-cors>=4.0
flask-socketio>=5.3
openai>=1.0
requests>=2.28
python-dotenv>=1.0
sqlalchemy>=2.0
reportlab>=4.2

# 规则引擎
pyyaml>=6.0

# 异步任务队列（可选，无 Redis 时自动降级到 threading）
celery>=5.3
redis>=5.0

# 向量数据库（知识图谱 + RAG）
chromadb>=0.4.0

# SmartAttack（智攻）v3.1 — 使用说明

> 基于大语言模型的 API 自动化渗透测试系统  
> "高级创新创业"课程项目 | 2026-06

---

## 目录

1. [项目结构](#1-项目结构)
2. [环境要求](#2-环境要求)
3. [快速开始（一键演示）](#3-快速开始一键演示)
4. [分步启动详解](#4-分步启动详解)
5. [使用流程](#5-使用流程)
6. [常见问题](#6-常见问题)

---

## 1. 项目结构

```
smart_attacking_project/
├── smart_attacking/         # 🔧 后端核心引擎 (Flask + AI)
│   ├── scanner.py           # Flask 主入口, REST API
│   ├── ai_engine.py         # AI 分析引擎 (业务分析/攻击方案/结果评估)
│   ├── attacker.py          # HTTP 攻击执行引擎 (并发发包)
│   ├── task_runner.py       # 异步任务执行器 (后台线程)
│   ├── report_generator.py  # PDF 安全报告生成器
│   ├── shadow_api.py        # 影子 API 检测引擎
│   ├── storage_db.py        # 数据库存储层 (SQLAlchemy + SQLite)
│   ├── models/              # 数据模型 + LLM 提供者抽象层
│   ├── .env                 # ⚠️ 环境配置 (含 API Key, 勿提交!)
│   ├── .env.example         # 配置模板
│   └── requirements.txt     # Python 依赖
│
├── smart-attack-ui/         # 🖥️ 前端界面 (React 19 + Vite)
│   ├── src/components/      # UI 组件 (20+ 组件)
│   └── package.json
│
├── vampi_mock/              # 🎯 靶场1: 轻量 Mock API (Python, 零依赖)
│   ├── server.py            # HTTP Server (端口 5000)
│   └── swagger.json         # OpenAPI 规范文档
│
├── target_api/              # 🎯 靶场2: NestJS 漏洞靶场 (TypeScript)
│   ├── src/app.controller.ts # 含 10+ 种漏洞类型
│   └── package.json
│
└── vampi_target/            # 🎯 靶场3: 完整 VAmPI 靶场 (Flask + SQLite)
    ├── app.py               # Flask 应用 (端口 5000)
    └── swagger.json
```

---

## 2. 环境要求

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| Python | 3.10+ | 后端引擎 + 靶场1/3 |
| Node.js | 18+ | 前端 + 靶场2 (NestJS) |
| npm | 9+ | 包管理 |
| Git | 任意 | 靶场2 的 NestJS 项目有 git 仓库 |
| DeepSeek API Key | — | 必需！AI 分析依赖大模型 |

---

## 3. 快速开始（一键演示）

这是最简化的启动方式，只需 **2 个终端**。

### 步骤 A：启动靶场 + 后端

```bash
# 终端 1 — 项目根目录

# 1. 激活虚拟环境（如果已创建）或直接使用系统 Python
# Windows:
venv\Scripts\activate
# Linux/Mac:
# source venv/bin/activate

# 2. 安装后端依赖（仅首次）
pip install -r smart_attacking/requirements.txt

# 3. 启动漏洞靶场（Python Mock，端口 5000）
python vampi_mock/server.py

# 4. 【新开终端】启动 SmartAttack 后端引擎（端口 8888）
python -m smart_attacking.scanner
```

### 步骤 B：启动前端

```bash
# 终端 3 — 项目根目录

cd smart-attack-ui

# 安装前端依赖（仅首次）
npm install

# 启动开发服务器
npm run dev
```

### 步骤 C：开始扫描

1. 浏览器打开 `http://localhost:5173`
2. 左侧「目标 Swagger 地址」输入框中已有默认地址：
   ```
   http://127.0.0.1:5000/api/swagger.json
   ```
3. 点击 **「🚀 启动 AI 渗透扫描」**
4. 等待约 30~60 秒，查看完整的 5 阶段扫描结果

---

## 4. 分步启动详解

### 4.1 配置 API Key（**必须**）

编辑 `smart_attacking/.env` 文件，填入你的 DeepSeek API Key：

```ini
DEEPSEEK_API_KEY=sk-你的APIKey
DEEPSEEK_MODEL=deepseek-chat
```

> 获取 Key：访问 [platform.deepseek.com](https://platform.deepseek.com) 注册并创建 API Key。
>
> 也可切换到 OpenAI / Claude，修改 `LLM_PROVIDER` 和对应 Key，详见 `.env.example`。

### 4.2 安装 Python 依赖

```bash
# 推荐先创建虚拟环境
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
# source venv/bin/activate

# 安装依赖
pip install -r smart_attacking/requirements.txt
```

依赖说明：
- `Flask` + `flask-cors` — Web 框架
- `openai` — DeepSeek / OpenAI API 客户端
- `requests` — HTTP 攻击发包
- `sqlalchemy` — 数据库 ORM
- `reportlab` — PDF 报告生成
- `python-dotenv` — 加载 .env 配置

### 4.3 选择并启动一个靶场

项目提供了 **3 个漏洞靶场**，任选其一即可：

#### 靶场 A：VAmPI Mock（推荐，最简单）

```bash
python vampi_mock/server.py
# 启动在 http://127.0.0.1:5000
# Swagger: http://127.0.0.1:5000/api/swagger.json
```

- ✅ 零依赖，Python 标准库即可
- ✅ 内置 7 种漏洞：越权、批量赋值、信息泄露、暴力破解、逻辑绕过、调试端点暴露、无认证密码重置
- ✅ 内存数据库，重启即恢复

#### 靶场 B：NestJS 漏洞靶场

```bash
cd target_api
npm install
npm run start:dev
# 启动在 http://localhost:3000
# Swagger: http://localhost:3000/api/swagger.json
```

- ✅ 10+ 种漏洞：BOLA、Mass Assignment、敏感信息泄露、暴力破解、业务逻辑绕过、IDOR、SSRF、命令注入、开放重定向
- ✅ TypeScript + NestJS 企业级框架
- ⚠️ 需要 Node.js 18+

#### 靶场 C：完整 VAmPI 靶场

```bash
cd vampi_target
pip install -r requirements.txt
python app.py
# 启动在 http://0.0.0.0:5000
# Swagger: http://localhost:5000/swagger.json (注意路径不同!)
```

- ✅ 完整的 Flask REST API
- ✅ JWT 认证、SQLite 数据库
- ⚠️ 依赖较多，需安装 `connexion[swagger-ui]` 等

### 4.4 启动后端引擎

```bash
# 方式一：在项目根目录（推荐）
python -m smart_attacking.scanner

# 方式二：在 smart_attacking 目录内
cd smart_attacking
python scanner.py
```

启动成功标志：
```
[INFO] smart_attack — 智攻后端引擎 v3.1 (模块化架构) 启动: http://127.0.0.1:8888
[INFO] smart_attack.llm_provider — LLM Provider 已初始化: deepseek / deepseek-chat
[INFO] smart_attack.database — 数据库表初始化完成
```

健康检查：浏览器访问 `http://127.0.0.1:8888/health`

### 4.5 启动前端界面

```bash
cd smart-attack-ui
npm install    # 仅首次
npm run dev
# 启动在 http://localhost:5173
```

---

## 5. 使用流程

### 5.1 一次完整的 AI 渗透扫描

SmartAttack 的扫描流水线有 **5 个阶段**：

| 阶段 | 名称 | 说明 |
|------|------|------|
| Phase 1 | 抓取 Swagger | 从目标 URL 拉取 OpenAPI 文档 |
| Phase 2 | AI 业务分析 + 攻击方案 | AI 深度分析业务逻辑，生成 5~8 组攻击 |
| Phase 3 | 并发攻击执行 | 并发发送所有攻击请求 |
| Phase 4 | AI 结果分析 + 后续攻击 | AI 评估响应，生成 3~5 组精炼攻击 |
| Phase 5 | 安全评估 + 持久化 | 综合评分 (OWASP + CVSS)，保存到数据库 |

在界面上的操作：

1. **输入目标 URL** — 左侧边栏填入靶场 Swagger 地址
2. **选择模型** — 默认 DeepSeek，也可切 OpenAI/Claude/自定义
3. **（可选）配置认证** — 如果目标 API 需要 JWT/API Key/Basic Auth
4. **选择模式** — 勾选「异步模式」(推荐，可关闭页面)
5. **点击扫描** — 等待 AI 完成全流程分析
6. **查看结果** — 依次展开各阶段的卡片查看详情
7. **下载 PDF** — 点击「📄 下载 PDF 报告」

### 5.2 查看扫描历史

左侧边栏底部「扫描历史」面板：
- 列出所有历史扫描（按时间倒序）
- 支持搜索、筛选（按状态/评级）
- 点击任意记录查看详情
- **选择 2 条记录**可进行**扫描对比**（diff 新增/修复/未变漏洞）

### 5.3 影子 API 发现

切换到「🕵️ 影子API发现」Tab：
1. 输入目标 Swagger URL
2. 粘贴流量日志（支持 HAR / JSON 数组 / URL 列表）
3. 点击检测 — 系统自动对比文档与流量，找出未登记的隐藏端点
4. 可对发现的影子 API 直接发起安全扫描

### 5.4 API 端点一览

| 端点 | 方法 | 说明 |
|------|------|------|
| `/start_scan` | POST | 启动扫描（支持 async/sync 模式） |
| `/scans` | GET | 列出扫描历史 (分页/搜索/筛选) |
| `/scans/<id>` | GET | 获取扫描详情 |
| `/scans/<id>/status` | GET | 查询异步扫描实时状态 |
| `/scans/<id>/report` | GET | 下载 PDF 报告 (`?format=pdf` 或 `?format=json`) |
| `/scans/compare` | GET | 对比两次扫描 (`?a=<id1>&b=<id2>`) |
| `/models` | GET | 获取可用 AI 模型列表 |
| `/shadow_api/detect` | POST | 影子 API 检测 |
| `/shadow_api/scan` | POST | 对影子 API 发起扫描 |
| `/health` | GET | 健康检查 |

---

## 6. 常见问题

### Q1: 启动后端报错 `LLM_API_KEY 未配置`

编辑 `smart_attacking/.env`，确保 `DEEPSEEK_API_KEY=sk-xxx` 已正确填写。

### Q2: 前端页面空白或接口报 CORS 错误

- 确保后端 `http://127.0.0.1:8888` 已启动
- 后端已内置 Flask-CORS，默认允许所有来源
- 如果前端运行在其他端口，检查 `smart-attack-ui/src/components/helpers.js` 中的 `API_BASE`

### Q3: 扫描一直转圈，不返回结果

- **同步模式**下，AI 分析需要 30~60 秒，属于正常等待
- **异步模式**下，检查后端终端是否有错误日志
- 确认 DeepSeek API Key 有效且有余额
- 确认目标靶场已启动并可访问

### Q4: 如何切换 AI 模型？

前端侧边栏「模型选择」下拉菜单支持：
- **DeepSeek**: `deepseek-chat` / `deepseek-reasoner`
- **OpenAI**: `gpt-4o` / `gpt-4o-mini` / `gpt-4.1`
- **Anthropic Claude**: `claude-sonnet-4` / `claude-haiku-4-5` / `claude-opus-4-8`
- **自定义**: 任意 OpenAI 兼容端点（Ollama / vLLM / LM Studio 等）

切换后需在 `.env` 中配置对应的 `LLM_API_KEY`。

### Q5: 如何切换数据库？

默认使用 SQLite（`smart_attacking/storage/smartattack.db`）。  
切换到 PostgreSQL：
```ini
# .env 中添加
DATABASE_URL=postgresql://user:password@localhost:5432/smartattack
```

### Q6: 前端如何构建生产版本？

```bash
cd smart-attack-ui
npm run build
# 产出在 dist/ 目录，可用任意静态服务器托管
```

### Q7: 端口冲突怎么办？

- 靶场使用 5000 → 修改 `vampi_mock/server.py` 中的 `PORT`
- 后端使用 8888 → 修改 `.env` 中的 `SCANNER_PORT`
- 前端使用 5173 → Vite 自动选择下一个可用端口

---

## 快速参考卡

```bash
# ===== 最简启动 (3 个终端) =====

# 终端 1 — 启动靶场
python vampi_mock/server.py

# 终端 2 — 启动后端
python -m smart_attacking.scanner

# 终端 3 — 启动前端  
cd smart-attack-ui && npm run dev

# 浏览器打开 http://localhost:5173
# 输入 http://127.0.0.1:5000/api/swagger.json
# 点击「启动 AI 渗透扫描」
```

---

> 📧 有问题？检查后端终端日志、确认 API Key 有效、确保所有服务端口不冲突。
